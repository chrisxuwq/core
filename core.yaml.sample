log_level: "INFO"
bind: "0.0.0.0:5001"
global_timeout: 300s
connection_timeout: 10s
lock_timeout: 30s
cert_path: "/etc/eru/tls"
max_concurrency: 20

store: etcd

etcd:
    machines:
        - "http://127.0.0.1:2379"
    prefix: "/core"
    lock_prefix: "core/_lock"


docker:
    log:
      type: "json-file"
      config:
        "max-size": "10m"
    network_mode: "bridge"
    hub: "hub.docker.com"
    namespace: "projecteru2"
    local_dns: true

scheduler:
    maxshare: -1
    sharebase: 100

virt:
    version: "v1"
